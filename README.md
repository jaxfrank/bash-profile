# Bash Profile

This is repository for my bash configuration. It contains aliases for various commands to make life in the terminal a little better.

To use add the following lines to `.bashrc`

```bash
export CODE_DIR=${HOME}/path/to/code/folder
export BASH_PROFILE_DIR=${CODE_DIR}/path/to/bash-profile
source ${BASH_PROFILE_DIR}/profile.sh
```
